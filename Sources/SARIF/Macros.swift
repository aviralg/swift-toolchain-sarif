/*
@attached(member, names: arbitrary)
@attached(extension, names: named(MergeKey), named(mergeKey), conformances: Mergeable)
public macro Mergeable() = #externalMacro(module: "RatchetMacros", type: "MergeableMacro")
*/
